run = "pip install -r requirements.txt && python app.py"
entrypoint = "app.py"
modules = ["python-3.11"]

[deployment]
run = ["python", "app.py"]

[[ports]]
localPort = 5000
externalPort = 80

[nix]
packages = ["glibcLocales"]

[agent]
expertMode = true
